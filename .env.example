# Copie para .env e preencha o que for usar.

# --- Fluxo de imagem (opcional) ---
# Só se usar o backend de imagem "api" (gpt-image) em vez do Codex grátis.
OPENAI_API_KEY=

# --- Fluxo de VÍDEO UGC ---
# fal.ai — motor de animação "api" e lip-sync (https://fal.ai/dashboard/keys)
FAL_KEY=
# ElevenLabs — voz PAGA natural em PT-BR (elevenlabs.io > perfil > API key). Opcional.
ELEVEN_API_KEY=
